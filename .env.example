# Copy to .env.local and fill in to connect a real backend.
# When VITE_API_BASE_URL is set, the in-app "Live Database" toggle will call this API.
# Leave empty to run fully on mock data.
VITE_API_BASE_URL=
VITE_API_KEY=
